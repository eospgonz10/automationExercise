webdriver {
  driver = edge
  timeouts {
    implicitlywait = 10000
    fluentwait = 10000
  }
}

#headless.mode = true

webdriver {
  capabilities {
    browserName = "MicrosoftEdge"
    "ms:edgeOptions" {
      args = ["start-maximized","test-type", "ignore-certificate-errors",
        "incognito", "disable-infobars", "disable-gpu", "disable-default-apps", 
        "disable-popup-blocking", "remote-allow-origins=*"]
    }
  }
}

# Configuración específica por ambiente
environments {
  default {
    webdriver.base.url = "https://automationexercise.com/"
  }
  dev {
    webdriver.base.url = "https://automationexercise.com/"
  }
  qa {
    webdriver.base.url = "https://automationexercise.com/"
  }
}

# Credenciales (usar variables de entorno en producción)
credentials {
  default {
    user.email = "prueba1@udea.edu.co"
    user.password = "Prueba123"
  }
}

# Configuración de Serenity
serenity {
  encoding = "UTF-8"
  compress.filenames = true
  take.screenshots = FOR_EACH_ACTION
  test.root = "org.com.screenplay.project"
  console.colors = true
  logging = VERBOSE
  
  # Reportes
  report {
    accessibility = true
    tagtypes = "feature, capability"
  }
}

# Configuración de paralelización
junit {
  parallel.enabled = true
  parallel.mode = classes
  parallel.config.fixed.parallelism = 3
}
